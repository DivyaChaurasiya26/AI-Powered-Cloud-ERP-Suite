name: Security Scan

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # ── TruffleHog: scan git history for committed secrets ───────────────────


  # ── Snyk: scan npm dependencies for known vulnerabilities ────────────────

  # ── Trivy: scan Docker image for OS + package CVEs ──────────────────────
