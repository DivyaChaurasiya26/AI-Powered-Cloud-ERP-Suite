apiVersion: v1
kind: Service
metadata:
  name: ml-service
spec:
  type: ClusterIP
  selector:
    app: erp-ml
  ports:
    - port: {{ .Values.mlService.port }}
      targetPort: {{ .Values.mlService.port }}
